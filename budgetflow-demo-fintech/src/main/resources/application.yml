spring:
  application:
    name: budgetflow-demo-fintech

budgetflow:
  enabled: true
  default-budget: 250ms
  demo:
    dataset: seed/default
  planner:
    profile: default
  runtime-signals:
    enabled: false
    include-default-provider: true
