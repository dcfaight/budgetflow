# Initial issues and project board plan

## Initial issues

1. **Scaffold Gradle Kotlin DSL multi-module project** (`setup`, `build`)
2. **Add budgetflow-core v1 interfaces and policy model** (`core`, `api`)
3. **Add Spring Boot auto-configuration and LatencyBudget annotation** (`spring`, `autoconfigure`)
4. **Create fintech dashboard demo application** (`demo`, `fintech`)
5. **Integrate policy engine decisions into executor orchestration** (`core`, `policy`, `execution`)
6. **Add degradation metadata and response reporting** (`metadata`, `observability`)
7. **Write benchmark plan for naive vs adaptive dashboard execution** (`benchmarks`, `research`)

## Project board plan

### Backlog

- benchmark module
- duration parsing utility enhancements
- actual omission/fallback policy enforcement
- Micrometer integration
- response headers for degradation
- demo scenarios

### In Progress

- scaffold
- core APIs
- demo app

### Done

- concept
- repo created
- initial architecture drafted
